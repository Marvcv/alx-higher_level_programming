#!/bin/bash

# This script executes a Python file using Python 3.

# Check if the PYFILE variable is set
if [ -z "$PYFILE" ]; then
    echo "PYFILE variable is not set"
    exit 1
fi

# Execute the Python script using Python 3 interpreter
python3 "$PYFILE"

